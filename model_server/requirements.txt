# Default dependency set used by OwlSpotlight's Python server.
# The base dependencies live in requirements/base.txt.  Torch is split out so GPU builds can override it.
-r requirements/base.txt
-r requirements/torch-cpu.txt
