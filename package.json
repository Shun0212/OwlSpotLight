{
  "name": "owlspotlight",
  "displayName": "owlspotlight",
  "description": "Highlight similar code snippets using semantic search.",
  "publisher": "Shun0212",
  "version": "0.5.9",
  "keywords": [
    "semantic search",
    "code search",
    "python",
    "function",
    "class",
    "method",
    "highlight",
    "embedding",
    "bert",
    "natural language",
    "similar code",
    "code navigation",
    "AI",
    "machine learning",
    "VSCode",
    "codex"
  ],
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": [
    "Programming Languages",
    "AI",
    "Machine Learning",
    "Other"
  ],
  "main": "./out/extension.js",
  "activationEvents": [],
  "icon": "media/owl.png",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "owlspotlight",
          "title": "OwlSpotlight",
          "icon": "media/owl.svg"
        }
      ]
    },
    "views": {
      "owlspotlight": [
        {
          "id": "owlspotlight.sidebar",
          "name": "Code Search",
          "type": "webview",
          "icon": "media/owl.svg"
        }
      ]
    },
    "commands": [
      {
        "command": "owlspotlight.startServer",
        "title": "OwlSpotlight: Start Server"
      },
      {
        "command": "owlspotlight.stopServer",
        "title": "OwlSpotlight: Stop Server"
      },
      {
        "command": "owlspotlight.cancelEmbedding",
        "title": "OwlSpotlight: Cancel Indexing / Embedding"
      },
      {
        "command": "owlspotlight.searchCode",
        "title": "OwlSpotlight: Code Search"
      },
      {
        "command": "owlspotlight.findSimilarSelection",
        "title": "OwlSpotlight: Find Similar to Selection"
      },
      {
        "command": "owlspotlight.setupEnv",
        "title": "OwlSpotlight: Setup Python Environment"
      },
      {
        "command": "owlspotlight.clearCache",
        "title": "OwlSpotlight: Clear Cache"
      },
      {
        "command": "owlspotlight.removeVenv",
        "title": "OwlSpotlight: Remove Virtual Environment"
      },
      {
        "command": "owlspotlight.generateAgentSetup",
        "title": "OwlSpotlight: Generate Agent Setup"
      },
      {
        "command": "owlspotlight.chooseBackend",
        "title": "OwlSpotlight: Open Search Backend Settings"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "OwlSpotlight Settings",
      "properties": {
        "owlspotlight.modelName": {
          "type": "string",
          "default": "Shuu12121/NightOwl-CodeEmbedding",
          "description": "Hugging Face model name used for embeddings"
        },
        "owlspotlight.batchSize": {
          "type": "number",
          "default": 2,
          "minimum": 1,
          "description": "Batch size for code embedding (Python server). Larger values may speed up indexing but use more memory."
        },
        "owlspotlight.cacheSettings": {
          "type": "object",
          "title": "Cache Settings",
          "description": "Configure cache behavior for better performance",
          "properties": {
            "autoClearCache": {
              "type": "boolean",
              "default": false,
              "description": "Automatically clear server cache when starting the server (clears .owl_index directory)"
            },
            "autoClearLocalCache": {
              "type": "boolean",
              "default": false,
              "description": "Automatically clear local cache files when starting the server"
            },
            "cachePath": {
              "type": "string",
              "default": "",
              "description": "Custom cache directory path (leave empty to use default model_server directory)"
            },
            "clearCacheOnCommand": {
              "type": "boolean",
              "default": false,
              "description": "Manual cache clear trigger - Check this box to clear cache immediately (.owl_index directory and local cache files). Setting will automatically return to unchecked after execution."
            }
          }
        },
        "owlspotlight.environmentSettings": {
          "type": "object",
          "title": "Environment Settings",
          "description": "Configure Python environment setup behavior",
          "properties": {
            "autoRemoveVenv": {
              "type": "boolean",
              "default": false,
              "description": "Automatically remove existing virtual environment before setup. WARNING: This will delete all installed packages!"
            },
            "pythonVersion": {
              "type": "string",
              "default": "3.11",
              "description": "Python version or interpreter path for uv to use when creating the virtual environment"
            }
          }
        },
        "owlspotlight.enableJapaneseTranslation": {
          "type": "boolean",
          "default": false,
          "description": "Translate Japanese search queries to English before searching"
        },
        "owlspotlight.geminiApiKey": {
          "type": "string",
          "default": "",
          "description": "Legacy Gemini API key. Use Gemini Search → API key & data sharing; existing values migrate to VS Code SecretStorage."
        },
        "owlspotlight.geminiModel": {
          "type": "string",
          "default": "gemini-3.8-flash",
          "enum": [
            "gemini-3.8-flash",
            "gemini-3.5-flash-lite",
            "gemini-3.5-flash"
          ],
          "enumDescriptions": [
            "Default for agentic search and translation.",
            "Lighter Flash-Lite model.",
            "Previous Flash model."
          ],
          "description": "Gemini model used for agentic search and Japanese query translation"
        },
        "owlspotlight.serverHost": {
          "type": "string",
          "default": "127.0.0.1",
          "description": "Host/IP address the OwlSpotlight search server binds to and the extension connects to. Default 127.0.0.1 (localhost). Note: codex/MCP integration expects 127.0.0.1:8000 unless OWLSPOTLIGHT_SERVER_URL is set. Restart the server after changing this."
        },
        "owlspotlight.serverPort": {
          "type": "number",
          "default": 8000,
          "minimum": 1024,
          "maximum": 65535,
          "description": "Local server port for the OwlSpotlight search server. If the port is busy, the next available port is used automatically. Restart the server after changing this."
        },
        "owlspotlight.autoStartServer": {
          "type": "boolean",
          "default": false,
          "description": "Automatically start the search server when the extension activates (requires .venv to be set up)"
        },
        "owlspotlight.autoIndexOnFileChange": {
          "type": "boolean",
          "default": true,
          "description": "Automatically refresh the incremental index when supported source files are created, changed, or deleted while the server is running"
        },
        "owlspotlight.highlightColors": {
          "type": "object",
          "title": "Highlight Colors",
          "description": "Customize highlight colors for code navigation results. Use CSS rgba() format, e.g. rgba(255,140,0,0.18).",
          "properties": {
            "jumpLine": {
              "type": "string",
              "default": "rgba(255,200,0,0.35)",
              "description": "Color of the exact function definition line you jumped to (gold)"
            },
            "standaloneFunction": {
              "type": "string",
              "default": "rgba(255,140,0,0.18)",
              "description": "Background color for a standalone function body (orange)"
            },
            "classMethod": {
              "type": "string",
              "default": "rgba(0,140,255,0.18)",
              "description": "Background color for a class method body (blue)"
            },
            "classBody": {
              "type": "string",
              "default": "rgba(0,200,100,0.08)",
              "description": "Background color for the entire enclosing class body (subtle green)"
            },
            "classHeader": {
              "type": "string",
              "default": "rgba(0,200,100,0.20)",
              "description": "Background color for the class definition line (green)"
            }
          },
          "additionalProperties": false
        },
        "owlspotlight.enableAgenticSearch": {
          "type": "boolean",
          "default": false,
          "description": "Let Gemini refine searches and assess results. Sends queries, retrieved excerpts and requested source files to Google using your API key."
        },
        "owlspotlight.agenticMaxSearches": {
          "type": "number",
          "default": 3,
          "minimum": 1,
          "maximum": 6,
          "description": "Maximum local searches per agentic search, including keyword verification."
        },
        "owlspotlight.graph.sideBySide": {
          "type": "boolean",
          "default": true,
          "description": "Synchronize graph-node selection and the source editor on the right in both directions. Disable to explore nodes without opening or following source code."
        },
        "owlspotlight.graph.openOnResultClick": {
          "type": "boolean",
          "default": true,
          "description": "Show the dependency graph when clicking a function or method search result. Can also be toggled in OwlSpotlight Settings. Disable for code-only navigation; the Open dependency graph button remains available."
        },
        "owlspotlight.searchBackend": {
          "type": "string",
          "enum": [
            "ask",
            "python",
            "node-onnx"
          ],
          "enumItemLabels": [
            "ask (Choose on first setup)",
            "backend_server(python)",
            "node_onnx (Simple mode, no server)"
          ],
          "enumDescriptions": [
            "Choose between the Python backend server and server-free simple mode on first setup or search.",
            "Choose this for faster search using a supported GPU. Sets up a Python environment and starts a local backend search server. Falls back to simple mode if setup or startup fails.",
            "Choose this if you cannot set up a Python server or prefer to run everything within VS Code. Runs ONNX models on the CPU using Node.js inside the extension, without a backend server."
          ],
          "default": "ask",
          "description": "Choose how search runs: start a Python backend server, or use server-free simple mode within VS Code. Your choice is saved for future sessions."
        },
        "owlspotlight.onnxModel": {
          "type": "string",
          "enum": [
            "Shuu12121/NightOwl-CodeEmbedding-35M",
            "Shuu12121/NightOwl-CodeEmbedding"
          ],
          "default": "Shuu12121/NightOwl-CodeEmbedding-35M",
          "description": "Simple-mode model. 35M: 384 dimensions, INT8 35 MB / FP32 137 MB. Original: 768 dimensions, INT8 152 MB / FP32 604 MB. Downloads once and caches locally."
        },
        "owlspotlight.onnxDtype": {
          "type": "string",
          "enum": [
            "q8",
            "fp32"
          ],
          "default": "q8",
          "description": "Simple-mode ONNX precision. Models and precisions use separate embedding caches."
        },
        "owlspotlight.onnxLocalFilesOnly": {
          "type": "boolean",
          "default": false,
          "description": "Use only previously downloaded ONNX files. Search fails with a missing-file message if the selected model is not cached."
        }
      }
    }
  },
  "menus": {
    "editor/context": [
      {
        "command": "owlspotlight.findSimilarSelection",
        "when": "editorHasSelection",
        "group": "navigation@10"
      }
    ],
    "view/item/context": [
      {
        "command": "owlspotlight.startServer",
        "when": "view == owlspotlight-actions",
        "group": "inline"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test",
    "test:unit": "npm run compile && node --test src/test/*.test.cjs",
    "test:onnx": "npm run compile && node scripts/onnxSmoke.cjs"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.100.0",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.25.1",
    "typescript": "^5.8.3"
  },
  "overrides": {
    "mocha": {
      "serialize-javascript": "^7.0.5"
    },
    "sharp": "^0.35.4",
    "adm-zip": "^0.6.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Shun0212/owlspotlight.git"
  },
  "dependencies": {
    "@google/genai": "^1.52.0",
    "@huggingface/transformers": "4.2.0",
    "@iarna/toml": "^2.2.5",
    "@modelcontextprotocol/sdk": "1.30.0",
    "ignore": "7.0.5",
    "minimatch": "10.2.4",
    "tree-sitter-wasms": "0.1.13",
    "web-tree-sitter": "0.25.10"
  }
}
